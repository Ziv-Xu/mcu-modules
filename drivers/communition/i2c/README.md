# 存放i2c通信相关的代码文件
## 目前有两种i2c，soft_i2c_gen和soft_i2c_simple
- soft_i2c_gen最好配合着soft_i2c_adapter一起使用，在soft_i2c_adapter中进行函数调用来适配不同的函数名。目前还没有进行验证
- soft_i2c_simple则是直接在自身文件使用结构体就行，在外设对应的文件中使用宏定义替换即可（有些带有变量的可能较为复杂，且遇到一主多从的使用情况不太方便）
